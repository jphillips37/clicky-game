import React from 'react';
import "./style.css";

function Header () {
    return (
        <header>
            <h1>Clicky Game!</h1>
            <h2>Click on the images to earn points, but clicking a image twice ends the game!</h2>
        </header>
    );
};

export default Header